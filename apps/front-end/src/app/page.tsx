import { Button } from '@mui/material';
import React from 'react';

const page = () => {
  return (
    <div>
      <h1>landing page</h1>
      <Button variant="contained">MUI Button</Button>
    </div>
  );
};

export default page;
